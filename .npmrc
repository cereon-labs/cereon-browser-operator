# Auto-install peer dependencies (pnpm 10 default; explicit for older clients).
auto-install-peers=true
# Don't hard-fail installs on a dependency's Node engine range — CI pins the
# Node version via a matrix instead, and the bundler targets the browser anyway.
engine-strict=false
